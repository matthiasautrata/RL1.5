[project]
name = "adalbert-linkml"
version = "0.7.0"
description = "LinkML authoring schema for Adalbert data contracts"
requires-python = ">=3.11"
dependencies = [
    "linkml>=1.8",
    "linkml-runtime>=1.8",
]
