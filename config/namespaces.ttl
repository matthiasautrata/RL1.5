# =============================================================================
# Adalbert Namespace Registry
# =============================================================================
# Authoritative source for all Adalbert namespaces.
# Import this file to ensure consistent prefix declarations.
#
# Adalbert is a proper ODRL 2.2 profile. The odrl: namespace provides
# all standard constructs (Rule types, Policy types, Constraint types,
# operators, core properties). The adalbert: namespace is for extensions
# only (lifecycle state, deadline, contracts, hierarchy, resolution,
# runtime references, logical negation).
# =============================================================================

# Primary — ODRL 2.2 (provides all standard terms)
@prefix odrl:    <http://www.w3.org/ns/odrl/2/> .

# Adalbert extensions
@prefix adalbert:      <https://vocabulary.bigbank/adalbert/> .
@prefix adalbertsh:    <https://vocabulary.bigbank/adalbert/shapes/> .
@prefix adalbert-due:  <https://vocabulary.bigbank/adalbert/due/> .

# External vocabularies
@prefix dcon:    <https://vocabulary.bigbank/dcon/> .
@prefix skos:    <http://www.w3.org/2004/02/skos/core#> .
@prefix rdf:     <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:    <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl:     <http://www.w3.org/2002/07/owl#> .
@prefix xsd:     <http://www.w3.org/2001/XMLSchema#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov:    <http://www.w3.org/ns/prov#> .
@prefix sh:      <http://www.w3.org/ns/shacl#> .
@prefix prof:    <http://www.w3.org/ns/dx/prof/> .

# =============================================================================
# NAMESPACE DOCUMENTATION
# =============================================================================
#
# odrl:          ODRL 2.2 — provides Rule, Policy, Constraint types,
#                operators, and core properties (action, target, assignee, etc.)
# adalbert:      Adalbert extensions only (State, deadline, DataContract,
#                Subscription, partOf, memberOf, resolutionPath,
#                RuntimeReference, rightOperandRef, not)
# adalbertsh:    SHACL validation shapes
# adalbert-due:  Data use profile (all operands, actions, concept values)
#
# DCON alignment mappings live in ontology/adalbert-dcon-alignment.ttl
# using the dcon: prefix for external DCON references.
#
# =============================================================================
