# Minimal example — read-only reference data

id: ref-data-readonly
label: Reference Data Read-Only
provider: data-platform-team
target:
  - reference-data

permissions:
  - action: read

prohibitions:
  - action: modify
